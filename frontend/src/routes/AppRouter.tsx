import { createBrowserRouter } from "react-router-dom";

const AppRouter = createBrowserRouter([
  {
    path: "/",
    element: <h1>Welcome to TeamFusion AI 🚀</h1>,
  },
]);

export default AppRouter;